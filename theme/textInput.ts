export type TextInputTheme = {
  padding: {
    horizontal: number;
    vertical: number;
  };
};

const textInput: TextInputTheme = {
  padding: {
    horizontal: 10,
    vertical: 10,
  },
};

export default textInput;

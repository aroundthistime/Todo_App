// import React, {useState} from 'react';
// import styled from "@emotion/native";

// type Props = {
//   children?: React.ReactNode;
//   initialOptionValue: any;
// };

// const Options = ({children, initialOptionValue}: Props) => {
//   const [selectedOption, setSelectedOption] = useState(initialOptionValue);

//   return (
//       <Options.Container>
//           {children.map(child => (
//               React.cloneElement(child, {
//                   isSelected :
//               })
//           ))}
//       </Options.Container>
//   )
// };

// Options.Container = styled.View`
//     flex-direction : row;
// `

export {};
